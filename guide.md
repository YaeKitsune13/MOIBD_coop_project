# 🛠 Инструкция по командной работе с Jupyter Notebook (.ipynb) в Git

Работа с ноутбуками в команде — это сложно из-за формата JSON. Чтобы проект не «развалился» при слиянии веток, следуйте этим правилам.

---

## 1. Подготовка (сделать один раз)
Чтобы Git не сходил с ума от изменений в метаданных, установите **nbdime**. Это инструмент, который понимает структуру `.ipynb`.

1. В терминале выполните:
   ```bash
   pip install nbdime
   nbdime config-git --enable --global
   ```
2. Создайте в корне проекта файл `.gitignore` и добавьте туда:
   ```text
   .ipynb_checkpoints/
   __pycache__/
   ```

---

## 2. Золотое правило коммита
**Перед каждым сохранением и коммитом файла:**
1. В верхнем меню Jupyter: `Kernel` -> `Restart & Clear All Outputs`.
2. Сохраните файл (`Ctrl + S`).
*Это удалит графики и номера запусков, оставив только чистый код. Так вероятность конфликтов снизится на 90%.*

---

## 3. Рабочий процесс в GitHub Desktop

### Создание ветки под задачу
Никогда не работайте в ветке `main` напрямую!
1. В GitHub Desktop: Кнопка **Current Branch** -> **New Branch**.
2. Дайте название (например: `feature-data-cleaning` или `user-alex-visuals`).
3. Нажмите **Publish branch**, чтобы она появилась на GitHub.

### Сохранение изменений
1. Напишите код в Jupyter.
2. Очистите выводы (пункт 2 этой инструкции).
3. В GitHub Desktop опишите изменения в поле *Summary* и нажмите **Commit to [название вашей ветки]**.
4. Нажмите **Push origin**, чтобы отправить код в облако.

---

## 4. Слияние изменений (Pull Request)
Когда задача в вашей ветке готова:
1. Перейдите на сайт GitHub в ваш репозиторий.
2. Нажмите **Compare & pull request**.
3. Напишите, что вы сделали, и назначьте коллег для проверки.
4. Если конфликтов нет — жмите **Merge pull request**.

---

## 5. Что делать, если возник Merge Conflict?

Если GitHub Desktop пишет **"1 conflicted file"**:

1. **Самый простой путь:** Нажать кнопку `Abort Merge` и договориться с коллегой, кто первый закинет изменения.
2. **Путь через nbdime (если уже установили):**
   - Откройте терминал в папке проекта.
   - Введите: `nbmerge-web`
   - В браузере откроется окно, где можно выбрать, какие ячейки оставить (свои или чужие).
3. **Путь через GitHub Desktop:**
   - Выберите `Use the modified file from [Your Branch]` (если ваш код важнее).
   - Или `Use the modified file from [Main]` (если вы хотите откатиться к версии из главной ветки).

---

## 🚀 Краткий чек-лист (TL;DR)
1. **Создал ветку** (New Branch) -> Работаешь.
2. **Очистил выводы** (Clear All Outputs) -> Сохранил.
3. **Сделал коммит** (Commit) -> Отправил (Push).
4. **Сделал Pull Request** на GitHub.
5. **Удалил ветку** после слияния.